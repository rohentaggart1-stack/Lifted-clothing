const count=document.getElementById('count');const toast=document.getElementById('toast');let bag=0;document.querySelectorAll('.add').forEach(button=>button.addEventListener('click',()=>{bag+=1;count.textContent=bag;toast.textContent='ADDED TO BAG';toast.classList.add('show');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>toast.classList.remove('show'),1200)}));document.getElementById('bagButton').addEventListener('click',()=>{toast.textContent=bag?`BAG: ${bag} ITEM${bag===1?'':'S'}`:'YOUR BAG IS EMPTY';toast.classList.add('show');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>{toast.textContent='ADDED TO BAG';toast.classList.remove('show')},1400)});